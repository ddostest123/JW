<div class="${wrapClass} " style="${style};" id="${id}" cellSpacing="0">
    <BUTTON class="${btnclass}" tabIndex="${tabindex}" type="button" hidefocus="true" atype="btn" title="${title}" style="${btnstyle}">
    	<i class="icon-${btnclass}"></i>
    	<span>${text}</span>
    </BUTTON>
</div>
<script>
    new Aurora.Button(${config});
    ${binding}
</script>